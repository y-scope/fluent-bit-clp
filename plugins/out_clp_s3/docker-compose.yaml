services:
  fluent-bit-clp:
    build:
      context: ../../
      dockerfile: plugins/out_clp_s3/Dockerfile
    volumes:
      - ~/.aws/credentials:/root/.aws/credentials
      - ./fluent-bit.yaml:/fluent-bit/etc/fluent-bit.yaml
      - ./var/out_clp_s3:/var/out_clp_s3
